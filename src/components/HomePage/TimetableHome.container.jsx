import React from 'react';
import TimetableHome from './TimetableHome';

const TimetableHomeContainer = () => {
  document.title = 'Timetable Maker';
  return <TimetableHome />;
};

export default TimetableHomeContainer;
